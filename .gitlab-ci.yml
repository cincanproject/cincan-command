stages:
  - test

unittests:
  stage: test
  image: "python:3-alpine"
  before_script:
    - apk add gcc --no-cache
    - pip3 install .
    - pip3 install -r test-requirements.txt
  script:
    - pytest